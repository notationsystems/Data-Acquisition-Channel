"""Notation Physical Commerce — the acquisition and record layer.

WHY THIS PACKAGE IS HERE AND NOT IN ITS OWN REPOSITORY. Sea Dog Terminal
is frozen at 5a6def1 pending its researcher session, so it cannot host
this; and creating a repository is an outward-facing act the founding
order did not authorise. `commerce/` therefore lives beside `session/` in
the acquisition channel, whose recon discipline PC-2 is an instance of.
That placement is a decision RECORDED (architecture/physical_commerce_founding.yaml),
not one a registry dictated. A package directory is cheap to relocate; the
PC-1 store split is not, which is why the split was taken first and the
address second.

THE LAYER RULE, inherited. `commerce/` may not import `epistemics`,
`science`, `bridge` or `boundary`. It names the evidence class it was
given and never restates which classes are admissible: that vocabulary is
owned elsewhere and a second copy of a closed vocabulary drifts from the
first.
"""
